#!/bin/bash
# wallpaper picker with dynamic color generation

if command -v swww >/dev/null 2>&1; then
    wallbin=swww
elif command -v awww >/dev/null 2>&1; then
    wallbin=awww
else
    noitfy-send "No wallpaper daemon found" "Please install awww or swww and try again"
    exit 1 
fi


current_theme=$(cat ~/.config/symphony/.current-theme 2>/dev/null || echo "dynamic")
if [[ "$current_theme" != "dynamic" ]]; then
	notify-send "Theme Locked" "Switch to Dynamic first"
	exit 0
fi

wallpaper_dir="$HOME/Pictures/Wallpaper"
wallpaper_params="--transition-fps 120 --transition-type grow --transition-duration 1 --transition-bezier 0.25,0.1,0.25,1.0 --transition-pos top-right"

mapfile -d '' wallpapers < <(find "$wallpaper_dir" -type f \( -iname "*.jpg" -o -iname "*.jpeg" -o -iname "*.png" -o -iname "*.gif" \) -print0)

if [[ ${#wallpapers[@]} -eq 0 ]]; then
	(action=$(notify-send -A "open=Get Wallpapers" --wait "No Wallpapers Found" "Add images to ~/Pictures/Wallpaper to use dynamic theming") && [[ "$action" == "open" ]] && xdg-open "https://github.com/vyrx-dev/Wallpapers") &
	exit 1
fi

# build rofi menu with thumbnails
menu() {
	for wp in "${wallpapers[@]}"; do
		name=$(basename "$wp")
		printf "%s\x00icon\x1f%s\n" "$name" "$wp"
	done | sort
}

"$wallbin" query &>/dev/null || "$wallbin"-daemon --format xrgb

choice=$(menu | rofi -i -dmenu -p "" -config ~/.config/rofi/custom-rofi/config-wallpaper.rasi)
[[ -z "$choice" ]] && exit 0

# find and apply selected wallpaper
for wp in "${wallpapers[@]}"; do
	if [[ "$(basename "$wp")" == "$choice" ]]; then
		"$wallbin" img "$wp" $wallpaper_params
		SCHEME_TYPE=$(grep '^SCHEME_TYPE=' ~/.config/symphony/matugen.conf 2>/dev/null | cut -d= -f2)
		matugen image "$wp" -m dark --source-color-index 0 -t "${SCHEME_TYPE:-scheme-content}"

		# Push fitting colormap to vividspektrum if it's running
		~/.config/hypr/scripts/update-spektrum-colormap &

		# update wallpaper symlink
		mkdir -p "$HOME/.config/symphony/current"
		ln -sf "$wp" "$HOME/.config/symphony/current/wallpaper"

		"${DOTFILES:-$HOME/dotfiles}/install/themes/symphony" reload
		restart-app swayosd-server

		# dolphin needs full restart to pick up new gtk theme
		pgrep -x dolphin >/dev/null && (
			pkill -x dolphin
			dolphin &>/dev/null &
		)

		notify-send -i "$wp" "Dynamic" "Colors from wallpaper"
		break
	fi
done
